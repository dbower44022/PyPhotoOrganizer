# Test Dependencies for PyPhotoOrganizer

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0      # Coverage reporting
pytest-timeout>=2.1.0  # Timeout support for long tests
pytest-xdist>=3.0.0    # Parallel test execution (optional)

# Test utilities
Pillow>=9.0.0          # Image generation (usually already installed)
piexif>=1.1.3          # EXIF manipulation (usually already installed)

# Optional: Enhanced reporting
pytest-html>=3.1.0     # HTML test reports
pytest-json-report>=1.5.0  # JSON test reports
